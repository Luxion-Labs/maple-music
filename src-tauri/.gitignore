# Generated by Cargo
# will have compiled files and executables
/target/

# The Android shell (gen/android) is committed so native shell customizations (e.g. the
# MainActivity safe-area/status-bar handling) persist across `tauri android build`. Build outputs
# and the downloaded native libs stay ignored via the nested .gitignore.
/gen/*
!/gen/android/

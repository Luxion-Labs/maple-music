[package]
name = "tauri-plugin-maple-discord"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = ["unstable"] }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
